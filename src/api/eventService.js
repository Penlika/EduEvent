import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const eventService = () => {
  return (
    <View>
      <Text>API quản lý sự kiện</Text>
    </View>
  )
}

export default eventService

const styles = StyleSheet.create({})