import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EventContext = () => {
  return (
    <View>
      <Text>
        Quản lý trạng thái sự kiện (danh sách sự kiện, thêm, xóa, sửa sự kiện)
        dùng cho flaslist sự kiện
      </Text>
    </View>
  );
};

export default EventContext;

const styles = StyleSheet.create({});
