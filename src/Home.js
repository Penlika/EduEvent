import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Home = () => {
  return (
    <View style={{
      flex:1,
      justifyContent:"center",
      alignItems:'center',
    }}>
      <Text style={{fontSize:16, fontWeight:'900'}}>After Splash will be Login & Account Registration </Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})