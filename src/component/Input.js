import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Input = () => {
  return (
    <View>
      <Text>Component Input (Form nhập liệu)</Text>
    </View>
  )
}

export default Input

const styles = StyleSheet.create({})