import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const useEvent = () => {
  return (
    <View>
      <Text>
        Hook useEvent giúp quản lý các sự kiện trong ứng dụng, dễ dàng thêm,
        sửa, xóa sự kiện. dùng eventContext ở đây
      </Text>
    </View>
  );
};

export default useEvent;

const styles = StyleSheet.create({});
